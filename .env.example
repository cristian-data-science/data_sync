# Configuración para conexión OData a Dynamics 365
# Copia este archivo como .env y completa con tus credenciales

# Azure AD - Credenciales de la aplicación
CLIENT_ID=tu-client-id-aqui
CLIENT_SECRET=tu-client-secret-aqui

# Azure AD - URLs de autenticación
TOKEN_URL=https://login.microsoftonline.com/TU-TENANT-ID/oauth2/v2.0/token
SCOPE_URL=https://TU-DYNAMICS-INSTANCE.operations.dynamics.com/.default

# Dynamics 365 - URL base de OData
BASE_URL=https://TU-DYNAMICS-INSTANCE.operations.dynamics.com/data

# Configuración de la entidad a consultar
ENTITY_NAME=PdSalesVSCostProcesseds
FILTER_FIELD=SalesId

# ⚠️ IMPORTANTE: 
# - FILTER_FIELD debe ser el nombre EXACTO del campo en Dynamics 365
# - OData es case-sensitive: 'SalesId' ≠ 'SALESID' ≠ 'salesId'
# - Nombre correcto más común: SalesId (con 'S' mayúscula e 'Id' con 'I' mayúscula)

# NOTAS:
# - Reemplaza TU-TENANT-ID con el ID de tu tenant de Azure
# - Reemplaza TU-DYNAMICS-INSTANCE con tu instancia de Dynamics 365
# - El CLIENT_ID y CLIENT_SECRET los obtienes del registro de la app en Azure AD
# - No compartas este archivo con las credenciales reales (.env)

# Snowflake Configuration
SNOWFLAKE_ACCOUNT=your-account.snowflakecomputing.com
SNOWFLAKE_USERNAME=your-username
SNOWFLAKE_PASSWORD=your-password
SNOWFLAKE_WAREHOUSE=your-warehouse
SNOWFLAKE_DATABASE=PATAGONIA
SNOWFLAKE_SCHEMA=CORE_TEST
SNOWFLAKE_ROLE=your-role

# Authentication Configuration
# JWT Secret Key (mínimo 32 caracteres, generado aleatoriamente)
# Genera uno nuevo con: openssl rand -base64 32
JWT_SECRET=genera-tu-secret-key-aqui-minimo-32-caracteres-aleatorios

# Admin Credentials
# Genera el hash con: node scripts/generate-password-hash.js tuPassword
# Luego convierte a base64: node scripts/encode-hash-base64.js <hash>
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH_BASE64=hash.bcrypt.en.base64

